{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build --censor-lib --strict",
    "test": "pulp test"
  },
  "devDependencies": {
    "pulp": "^9.0.1",
    "purescript": "^0.9.1",
    "purescript-psa": "^0.3.8",
    "rimraf": "^2.4.4"
  }
}
